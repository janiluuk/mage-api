# Backlog

## Library metadata
- Support tagging and rating of user video jobs/files, exposing tags/ratings in list endpoints and adding update endpoints (needed for `mage-app` `/browser` filters, context menu, and metadata panel).
